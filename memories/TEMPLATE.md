---
name: <short-kebab-case-slug-for-the-generalized-truth>
description: <one line stating the core rule — written so an agent scanning a memory index knows when to recall this>
metadata:
  type: feedback | reference | project | user
---

## Context / Trigger

<!-- When does this memory apply? Describe the situation, task type, or symptom
     that should make a developer or agent recall it. Use [BRACKET_PLACEHOLDERS]
     for anything the reader must map onto their own project, e.g.:
     "You are adding a background job to [YOUR_APP_NAME] and it uses
     [QUEUE_LIBRARY]..." -->

## Core Rule / Insight

<!-- The engineering truth, architectural pattern, or debugging lesson itself.
     Be concrete: named public tools, exact techniques, and external URLs are
     the payload — keep them. What must NOT appear here: local paths, private
     project names, personal names, dates, session IDs, or [[links]] to
     memories outside this library. -->

## Expected Outcome

<!-- What following the rule produces, and what ignoring it costs. This is how
     a reader judges whether the memory worked. -->
